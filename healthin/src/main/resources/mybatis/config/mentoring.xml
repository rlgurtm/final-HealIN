<?xml version="1.0" encoding="UTF-8"?>
<!-- Sql Mapper -->
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mentoring">
 	<select id="findByTrainerMatchingMemberList" parameterType="string" resultType="memberVO">
 	select id,nickname,birthdate,gender,address
 	from health_member h,
 		(select user_id from matching 
 		where trainer_id=#{value} and apply_result='Y' and accept_result='Y') m
 	where h.id=m.user_id
 	</select>
 	<select id="findByUserMatchingMemberList" parameterType="string" resultType="memberVO">
 	select id,nickname,birthdate,gender,address
 	from health_member h,
 		(select trainer_id from matching 
 		where user_id=#{value} and apply_result='Y' and accept_result='Y') m
 	where h.id=m.trainer_id
 	</select>
</mapper>
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
















