<?xml version="1.0" encoding="UTF-8"?>
<!-- Sql Mapper -->
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
 	<select id="login" resultType="memberVO" parameterType="map"  >
 		select 
 		id,password,nickname,name,birthdate,gender,address,tel,
 		e_mail as email,is_trainer as istrainer,withdrawal
        from health_member where id=#{id} and password=#{password}
 	</select>
</mapper>
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
















